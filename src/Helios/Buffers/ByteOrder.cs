namespace Helios.Buffers
{
    public enum ByteOrder
    {
        /// <summary>
        /// Default on all X86 systems; backgwards compatible with previous Helios defaults.
        /// </summary>
        LittleEndian = 0,
        BigEndian = 1
    }
}
